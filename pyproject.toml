[project]
name = "detective-utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["jsonpath-ng>=1.6"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
